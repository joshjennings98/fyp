name: 
test_network;

equations:
v += 0.04 * v * v + 5 * v + 140 - u + I,
u += a * (b * v - u);

threshold:
v >= 30;

reset:
v = v,
u += u;

parameters 1:
a = 0.02,
b = 0.2,
v = -65r,
u = 2r,
property fanin = 1000,
fraction = 0.8,
refractory = 0,
connectionProb = 1;

parameters 2:
a = 0.1r,
b = 0.2r,
v = -65,
u = 2,
property fanin = 1000,
fraction = 0.2,
refractory = 0,
connectionProb = 1;

number:
1000;

maxt:
2000;

type:
clocked
